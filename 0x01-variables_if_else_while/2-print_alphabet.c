#include <stdio.h>

/**
 * main -> Entry points
 * and print the last digits
 * return : always 0 
 */
int main(void)
{
	int ch;
	for (ch = 97; ch <= 122; ch++)
	{
		putchar(ch);
	}
	putshar(10); /* this is an ASCII code for new line*/

	return (0);
}
