#include <stdio.h>

/**
 * main -> entry points
 * and print the last digit
 * Return : always 0
 */
int main(void)
{
	int ch;
	for (ch = 48; ch <= 57; ch ch ++)
	{
		ptchar(ch);
	}
	putchar(10); /* this is an ascii code for new lines*/

	return (0);
}
