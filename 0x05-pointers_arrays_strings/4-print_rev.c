#include "main.h"
/**
 * print_rev - Use a pointer to change the value of memory
 * @s: pointer to s location
 *Return: Null/Void
 */
void print_rev(char *s);
{
	int = 0;

	while (*s)
	{
		i++;
		s++;
	}
	s--;
	for (; i > 0; i--)
	{
		putchar(*s);
		s--;
	}
	putchar('\n');
}
